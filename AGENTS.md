# Agent notes

- **Import layering on any `lib/**` edit:** Cursor should attach `.cursor/rules/lib-import-layering.mdc` under `lib/**/*.ts(x)` — **`lib/**` must not import `features/**`**. Keeps the ban visible even when `ARCHITECTURE_ESSENTIALS` is not in context (e.g. `lib/utils`, `lib/db`).
- **Schedule / allocation / Step 1–5 / Step 3 projection / algorithms:** `.cursor/rules/ARCHITECTURE_ESSENTIALS.mdc` applies under `lib/features/schedule/`, `lib/algorithms/`, `features/schedule/`, `components/allocation/`, and `app/(dashboard)/schedule/`. **`@`-mention** it (and `lib-import-layering.mdc` if needed) on long threads so rules are not dropped.
- **Schedule UI vs logic (paths, naming):** short stable map — [`docs/schedule-architecture-core.md`](docs/schedule-architecture-core.md). Full allocation / Step 1–5 / Step 3 rules: [`.cursor/rules/ARCHITECTURE_ESSENTIALS.mdc`](.cursor/rules/ARCHITECTURE_ESSENTIALS.mdc). Schedule page decomposition (phased checklist): [`docs/superpowers/plans/2026-04-22-schedule-page-client-decomposition-implementation-plan.md`](docs/superpowers/plans/2026-04-22-schedule-page-client-decomposition-implementation-plan.md) + spec [`docs/superpowers/plans/2026-04-22-schedule-page-client-decomposition-spec.md`](docs/superpowers/plans/2026-04-22-schedule-page-client-decomposition-spec.md).
- **Path alias:** `@/*` → repository root (`tsconfig.json` `compilerOptions.paths`).
