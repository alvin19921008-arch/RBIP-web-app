# Changelog 2

## Step 3 V2 (ranked slots) — 2026-04

**Revamped V2:** Ranked-slot Step 3.4 uses a continuity-friendly draft pass plus a **bounded, deterministic audit/repair** pass; tracker distinguishes draft / repair / extra-coverage; `selected_only` preserves base ranked slots while biasing PCA choice from Step 3.2/3.3. The V2 wizard preview materializes committed Step 3.2/3.3 picks into allocations **before** Step 3.4 so save state matches the sheet.

For objectives, defect kinds, and diagnostics, see **`docs/superpowers/specs/2026-04-06-floating-pca-ranked-slot-allocation-design.md`**.

**2026-04-10:** PCA tracker V1 freeze + V2 tooltip/model; `preStep34RoundedPendingFte`; duplicate-floating semantics aligned to Step 3.4; f76/f78/f79; V2 scarcity summary/dialog where changed. **Checkpoint:** f73–f82 green; extraction spec/plan (`2026-04-10-floating-pca-v1-v2-extraction-*`) added — rollback point before allocator split.

**2026-04-10 (later):** Floating PCA **V1/V2 code split**: legacy vs ranked engines in `floatingPcaLegacy/` and `floatingPcaV2/`; shared contracts + invalid-slot helper; inline `allocatePCA()` floating phase extracted; V2 provenance + ranked tracker summary derivations isolated; thin `pcaAllocationFloating` façade. Behavior-named exports unchanged.

**2026-04-11:** V2 fairness-floor (`repairAudit`/`repairMoves`, f84/f85); Step 3 ownership/fulfillment helper + V2 tooltip line (`step3FloatingFulfillmentSemantics`, f86/f87); `extraCoverageRuntime` marks extra only on true Step 3 surplus (f88).

**2026-04-12:** *(2nd draft — revamped V2 Step 3.2 preferred review.)* Status-first lane with stacked lane→detail; beak on the outer detail shell; wide `xl` two-column layout for steps 1–2 with horizontal outcome strip and clipped peek when there are ≥2 cards; Radix popover for “How to read statuses”; outcome rows as a compact CSS grid (rank · interval · PCA). Preview/copy in `lib/features/schedule/step32V2/`; UI in `components/allocation/step32V2/`; Step 3.2 commits via `committedStep3Assignments` with **legacy** preferences so Step 3.4 keeps the full preferred set; `FloatingPCAConfigDialogV2` + f65/f89–f92.

**2026-04-12 (later):** Step 3 **floating vs non-floating copy** and less clutter. **3.1** (`TeamPendingCard` + `FloatingPCAConfigDialogV2`): hero labeled **Pending floating**; rows **Avg**, **Raw floating** (`initialPendingFTE`), **Rounded** (cap), **Non-floating** (Step 2; no ambiguous “Assigned”); flat collapsible legend (“What the card numbers mean”, default collapsed). **3.2–3.4** UI: **Pending floating** / **Assigned floating** wording; Step 3.2 detail uses `assignedFloatingFte` from the same Step 3 committed-slot tally as 3.3/3.4. **3.4** detail header: removed redundant **Pending met** chip (status remains on the team strip); only floating badges top-right; `buildStep34TeamDetailViewModel` no longer emits the pending-met summary pill so secondary pills map without `slice(1)`.

**2026-04-12 (preferred / gym / repair):** Step 3.2 **scenario-first** preferred review: `preferredPcaStatuses` and `primaryScenario` on `Step32TeamReview` (`step32PreferredReviewModel`); per-name availability badges + `getStep32PreferredAvailabilityLabel` / `getStep32SaveEffectLabel` / `getStep32SaveSlotOnlyNearActionLabel` (`step32PreferredReviewCopy`); detail panel shows scenario summary above outcomes and clarifies that **Save** reserves only the selected slot for Step 3.4 (`Step32PreferredReviewDetailPanel`). **Canonical gym:** `GymUsageStatus` and `summary.gymUsageStatus` on `TeamAllocationLog` (`types/schedule.ts`); ranked V2 finalization sets it from `slotSelectionPhase === 'gym-last-resort'` and aligns legacy `gymSlotUsed` / `gymUsedAsLastResort` (`trackerSummaryDerivations`); Step 3.4 summary pills and V2 PCA tracker tooltip **Status** use the same resolution (`step34ViewModel`, `v2PcaTrackerTooltipModel`). **Repair:** bounded donor-donation path + donor-protection exports on `repairAudit`; direct donation candidates for B1/F1 in `repairMoves`; `gymLastResortCount` in lexicographic `compareScores` (`scoreSchedule`); **second** repair loop after `applyExtraCoverageRoundRobin` and tracker `repairAuditDefects` synced to post-extra audit (`allocator`). **Tests:** f93/f94 (Step 3.2 contracts, prior commit); f95–f99 (gym source-of-truth, avoid-gym invariant, bounded donation safe/harmful, post-extra re-audit); f84/f85 updated for repair context; focused bundle **f72, f84, f85, f92–f99** green. Implementation plan: `docs/superpowers/plans/2026-04-12-step32-step34-preferred-gym-and-repair-fixes-implementation-plan.md`.

**2026-04-13:** Surplus Part I **docs only** (backup before Task Group A): `/help/avg-and-slots` + Help Center Guides; shared Avg PCA formula popover (`avgPcaFormulaSteps`, `AvgPcaFormulaPopoverContent`) on dashboard + schedule; surplus **spec** — continuous vs `redistributableSlackSlots` cap, engineering **glossary** (stable code names), planned discreet lines for 3.1/3.4 noted; contract-table / impl-plan **Task A0b** deferred at capture (**shipped 2026-04-14**).

**2026-04-13 — *retrospective:*** Step 3 **contract reset Part 1** — unified `Step3ProjectionV2` (controller → page / dialog / PCABlock), avg + surplus provenance + pending-first rounded seed + non-floating breakdown; removed bootstrap debug ingest and dead rounded-slack path; duplicate-floating pair rule + tooltip pills; `f51` legacy gating + forward step nav when `stepStatus` lags rows; plan complete + `ARCHITECTURE_ESSENTIALS` projection blurb; **f100–f113**, **f115** green (`npx tsx`). **f114** added with Task A0b (2026-04-14).

**2026-04-14:** V2 **Task A0b** (surplus copy deck): Step 3.1 — shared-spare callout (plain language + `/help/avg-and-slots` + “Show details”) **only when** some active team has a positive `realizedSurplusSlotGrantsByTeam` (otherwise users still see the card legend and, if applicable, scarcity **post-need projection** copy only). Step 3.4 — **Raised target** / **Extra after needs** header chips, violet post-need micro-caption; `step34ViewModel` **Extra after needs** bullet under Why; `f114` regression.

**2026-04-14 (later):** Step 3 literacy polish — flat **harmonized strip** above “What the card numbers mean” (shared spare + **Extra after needs** preview when `standardProjectedExtraSlots > 0`, no duplicate in scarcity amber); **emerald** surplus numbers in “Show details”, **violet** for post-need counts (Step 3.1 preview + Step 3.4 Why bullet); **Extra after needs** styling unified on `violet-*` (PCABlock, tables, v1 dialog, tie color); `design-elements-commonality.mdc` semantic tokens. Step 3.4 detail: dropped surplus micro-caption and redundant header pills (preferred PCA / gym). Card legend + `TeamPendingCard` tooltips: **Raw floating** / **Rounded floating** copy (en dash **Avg – non-floating**). **Docs:** floating/non-floating **glossary** at `docs/glossary/step3-floating-nonfloating.md` (replaces superpowers contract-table path); `ARCHITECTURE_ESSENTIALS` + surplus/part1 plans point there.

**2026-04-15:** V2 **Task Group B** (surplus plan Part II): optional **ranked promotion** — separate `P1` / `detectOptionalRankedPromotionOpportunities` (not `B1`); bounded candidates (cross / same-team / same-PCA) after required repair, before extra coverage; `compareScores` promotion tie-break (`promotionTrueStep3RankScore`, `promotionTrueStep3PreferredPcaHits`); Step 3.2+3.3 **`committedStep3Anchors`** immutability in repair + promotion; tracker `repairReason` **`ranked-promotion`** + label in `pcaTrackerTooltip`. **Tests:** **f116–f120**, **f62** promotion comparator; plan checklist updated (`f111`–`f115` sketch → **f116**–**f120**).
